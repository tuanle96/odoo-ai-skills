[options]
; Used only by the integration workflow (.github/workflows/integration.yml).
; Points odoo at the Postgres service; the smoke test passes -d / --no-http itself.
db_host = localhost
db_port = 5432
db_user = odoo
db_password = odoo
list_db = True
without_demo = all
